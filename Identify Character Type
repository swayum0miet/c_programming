#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
int main() {
    char a;
    scanf("%c", &a);
    
    if (isalpha(a)) {
        printf("Alphabet");
    } else if (isdigit(a)) {
        printf("Digit");
    } else {
        printf("Symbol");
    }
    
    return 0;
}
